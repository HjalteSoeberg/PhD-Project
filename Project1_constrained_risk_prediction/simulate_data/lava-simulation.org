** logistic regression

#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes  
library(lava)
library(data.table)
m <- lvm(y~x)
distribution(m,~x) <- binomial.lvm(p=0.7)
distribution(m,~y) <- binomial.lvm(p=0.1)
regression(m,y~x) <- 0.8
d <- setDT(sim(m,100000))
d[,.(x=mean(x),y=mean(y))]
#+END_SRC

#+RESULTS[(2022-11-30 14:28:10) ceed39f499a3f976b181c960e88a58b5b0012b8f]:
:results:
         x      y
1: 0.70225 0.1677
:end:

#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes  
library(lava)
library(data.table)
m <- lvm(y~x)
distribution(m,~x) <- binomial.lvm(p=0.7)
distribution(m,~y) <- binomial.lvm(p=0.4)
regression(m,y~x) <- -0.7
d <- setDT(sim(m,100000))
d[,.(x=mean(x),y=mean(y))]
Publish::org(glm(y~x,data=d,family="binomial"))
#+END_SRC

#+RESULTS[(2022-11-30 12:12:30) 08d0cf97dd311a5f45a3da44560621587c7c4bd5]:
:results:
         x       y
1: 0.70088 0.29207
|  Variable | Units | OddsRatio |       CI.95 |  p-value |
|-----------+-------+-----------+-------------+----------|
|         x |       |      0.50 | [0.48;0.51] | < 0.0001 |
:end:


** Weibull shape, scale survreg
#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
library(survival)
library(lava)
m <- lvm()
distribution(m,"eventtime") <- coxWeibull.lvm(scale=1/100)
distribution(m,"censtime") <- coxWeibull.lvm(scale=1/100)
regression(m,eventtime~X+Z) <- c(log(2),log(.3))
regression(m,censtime~X+Z) <- c(log(1.2),log(0.2))
m <- eventTime(m,time~min(eventtime=1,censtime=0),"status")
d <- sim(m,1000)
# estimate semiparametric Cox model for event time hazard function
F <- coxph(Surv(time,status)~X+Z,data=d)
coef(F)
# estimate parametric Cox model for event time hazard function
F2 <- survreg(Surv(time,status)~X+Z,data=d)
-coef(F2)[-1]/F2$scale
# estimate semiparametric Cox model for censoring time hazard function
G <- coxph(Surv(time,status==0)~X+Z,data=d)
coef(G)
# estimate parametric Cox model for censoring time hazard function
G2 <- survreg(Surv(time,status==0)~X+Z,data=d)
-coef(G2)[-1]/G2$scale
#+END_SRC

#+RESULTS[<2014-07-16 07:50:04> 1b8a9bbd8edcb4cd9c0e4eba38330118c63c986a]:
:RESULTS:
Call:
survreg(formula = Surv(eventtime, status) ~ 1, data = d)

Coefficients:
(Intercept) 
   4.608933 

Scale= 1.001606 

Loglik(model)= -5609.6   Loglik(intercept only)= -5609.6
n= 1000
[1] -4.60517
:END:


#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
library(lava) 
m <- lvm(~t+c+x1+x2+x3+x4)
distribution(m,~x1) <- binomial.lvm(p=0.4)
distribution(m,~x2) <- binomial.lvm(p=0.8)
distribution(m,~x3) <- binomial.lvm(p=0.1)
distribution(m,~x4) <- binomial.lvm(p=0.5)
distribution(m,~t) <- coxExponential.lvm(rate=100)
distribution(m,~c) <- coxExponential.lvm(rate=100)
m <- eventTime(m,time~min(t=1,c=0),"event")
sim(m,50)
#+END_SRC

** Double parameter regression, regression in both Weibull parameters

weibull regression design shape parameter:

#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
library(prodlim)
library(lava)
library(mets)
m <- lvm()
## regression(m) <- y ~ z+v
regression(m,to="y",from=c("z","v")) <- c(0,0)
regression(m) <- s ~ exp(-0.5*z)
distribution(m,~z) <- binomial.lvm()
distribution(m,~cens) <- coxWeibull.lvm(scale=1)
distribution(m,~y) <- coxWeibull.lvm(scale=0.1,shape=~s)
eventTime(m) <- time ~ min(y=1,cens=0)
sim(m,4)
#+END_SRC

Simulating data that violates the proportional hazards assumption
#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
  library(lava)
  library(prodlim)
  library(mets)
  m <- lvm()
  regression(m) <- y ~ z+v
  regression(m) <- s ~ exp(0.6*x-0.5*z)
  distribution(m,~x+z) <- binomial.lvm()
  distribution(m,~cens) <- coxWeibull.lvm(scale=1)
  distribution(m,~y) <- a <- coxWeibull.lvm(scale=0.1,shape=~s)
  eventTime(m) <- time ~ min(y=1,cens=0)
  d <- sim(m,5e3)
  cc <- prodlim(Surv(time,status)~x+z,data=d)
  plot(cc,col=1:4,mark.time=FALSE)
#+END_SRC

#+RESULTS[(2026-03-12 13:22:22) 3f0a76a13914ab5bd7745265912137ba9d02b03d]:
:results:
:end:

#+RESULTS[<2016-02-08 08:42:36> 5e5550b5d2cc997fb71dbaef4fe66a0d46cffe5d]:
:RESULTS:
Loading required package: timereg
Loading required package: survival
Loading required package: lava
lava version 1.4.1.4

Attaching package: ‘lava’

The following objects are masked _by_ ‘.GlobalEnv’:

    expit, logit

mets version 1.1.1
Call:  coxph(formula = Surv(time, status) ~ strata(x, z), data = d)

Null model
  log likelihood= -6279.426 
  n= 5000
:END:

Og her eksempel med variance parameter der afhaenger af covariate:
#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
m <- lvm()
regression(m) <- y~x
regression(m) <- v~exp(x)
distribution(m,~y) <- function(n,mean,v) rnorm(n,mean,v^.5)
if (interactive()) plot(y~x,sim(m,1e3))
#+END_SRC

** U-shape and J-shape regression

#+ATTR_LATEX: :options otherkeywords={}, deletekeywords={}
#+BEGIN_SRC R  :results output raw drawer  :exports both  :session *R* :cache yes  
library(survival)
library(lava)
library(rms)
library(splines)
library(Greg)
m <- lvm()
distribution(m,"eventtime") <- coxWeibull.lvm(scale=1/100)
distribution(m,"censtime") <- coxWeibull.lvm(scale=1/100)
transform(m,X2~X) <- function(x){x^2}
regression(m,eventtime~X+X2+Z) <- c(0.4,log(2),log(.3))
m <- eventTime(m,time~min(eventtime=1,censtime=0),"status")
d <- sim(m,1000)
A <- coxph(Surv(time,status)~pspline(X)+Z,data=d)
fit.cph <- cph(Surv(time,status)~rcs(X)+Z,data=d)
fit.coxph <- coxph(Surv(time,status) ~ bs(X, 3) + Z, data = d)
plotHR(fit.coxph, term = "X", plot.bty = "o", xlim = c(-1, 1), xlab = "Age")
plotHR(fit.cph, term = "X", plot.bty = "o", xlim = c(-1, 1), xlab = "Age")
#+END_SRC

** Categorical

#+BEGIN_SRC R  :results output raw drawer  :exports code  :session *R* :cache yes  
library(lava)
library(data.table)
m <- lvm(~z)
m <- categorical(m,~x,K=4,p=c(0.24,0.2,0.3))
m <- categorical(m,"v",K=4,p=c(0.24,0.2,0.3))
transform(m,x0~x) <- function(x){1*(x$x==0)}
transform(m,x1~x) <- function(x){1*(x$x==1)}
transform(m,x2~x) <- function(x){1*(x$x==2)}
transform(m,x3~x) <- function(x){1*(x$x==3)}
regression(m,z~x0+x1+x2+x3) <- c(.5,-.8,.3,.1)
d=sim(m,1000)
setDT(d)
d[,x:=factor(x)]
glm(z~x,data=d)
#+END_SRC

#+RESULTS[(2023-07-04 13:57:01) 342a16b44c379ec44d75938335142104d647162a]:
:results:

Call:  glm(formula = z ~ x, data = d)

Coefficients:
(Intercept)           x1           x2           x3  
     0.4845      -1.3348      -0.1572      -0.4800  

Degrees of Freedom: 999 Total (i.e. Null);  996 Residual
Null Deviance:	    1214 
Residual Deviance: 985.5 	AIC: 2833
:end:




#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
library(lava)
m <- lvm()
regression(m) <- z~x
m <- categorical(m,~x,K=4,p=c(0.24,0.2,0.3))
distribution(m,"b") <- binomial.lvm(p=c(0.3))
with(sim(m,1e4),table(x))/1e4
with(sim(m,1e4),table(b))/1e4
d <- sim(m,1e4)
d$b <- factor(d$b,levels=c("0","1"),labels=c("ref","group 2"))
m <- categorical(m,y~z,K=3)
d <- sim(m,1e4,p=c('y~z:0'=3,'y~x:2'=4))
lm(y~factor(z)+factor(x),data=d)
with(d,table(x))/nrow(d)
#+END_SRC

based on latent normal 
   
#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
distribution(simModel,~U) <- normal.lvm()
transform(simModel,Z~U) <- function(x)cut(x,c(-Inf,-0.5,0.5,Inf),labels=c("low","medium","high"))
#+END_SRC

   
#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
m <- lvm() 
functional(m,y~age) <- function(x) -0.05*x+0.1*x^2
# eller
m <- lvm(y~age+age2)
transform(m,age2~age) <- function(x) x^2
d <- sim(m,10000,p=c("y~age"=-0.05,"y~age2"=0.001))
#+END_SRC

#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
library(lava)
m <- lvm(y1~x)
regression(m) <- y2~x
transform(m,time~y1+y2) <- function(arg) with(arg, pmin(y1,y2))
transform(m,event~y1+y2) <- function(arg) with(arg, y1<=y2)
transform(m,event~y1+y2) <- function(arg) with(arg, y1<=y2)
regression(m) <- z~event
d <- sim(m,10000)
plot(z~event,d)
#+END_SRC

#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
m <- lvm()
distribution(m,"a") <- normal.lvm(mean=40,sd=10)
regression(m,from="b",to="a") <- 5
summary(lm(a~b,data=sim(m,1000)))
m <- lvm()
distribution(m,"a") <- normal.lvm(sd=10)
intercept(m,"a") <- 40
regression(m,from="b",to="a") <- 5
summary(lm(a~b,data=sim(m,1000)))
#+END_SRC


#+BEGIN_SRC R  :results raw output replace  :exports results  :session *R* :cache yes 
library(lava)
m <- lvm(Y~X)
distribution(m,"Y") <- binomial.lvm()
intercept(m,"Y") <- -2.3
regression(m,from="X",to="Y") <- 1.4
d <- sim(m,2000)
summary(glm(Y~X,data=d,family="binomial"))
#+END_SRC

#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes  
m <- lvm()
lava::distribution(m,"A") <- lava::binomial.lvm()
regression(m,A~B) <- 0
intercept(m,"A") <- log(.1)
d <- sim(m,1000)
mean(d$A)
#+END_SRC


#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
library(lava)
m <- lvm(Y~X)
distribution(m,"Y") <- binomial.lvm()
regression(m,from="X",to="Y") <- -1.4
d <- sim(m,200)
#+END_SRC

glm(Y~X,data=d,family="binomial")
#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
library(lava)
m <- lvm(~Y+Z+eventtime+censtime+Noise)
distribution(m,"Y") <- binomial.lvm()
distribution(m,"eventtime") <- coxWeibull.lvm(scale=1/100)
distribution(m,"censtime") <- coxWeibull.lvm(scale=1/100)
regression(m,to="eventtime",from=c("Z")) <- -1.6
## regression(m,to="Y",from=c("Z")) <- log(5)
m <- eventTime(m,time~min(eventtime=1,censtime=0),"event")
d <- sim(m,10)
coxph(Surv(time,event)~Z,data=sim(m,1000))
#+END_SRC

#+RESULTS[<2013-05-31 11:44:11> 5434903d16de0c697509894d647a349b6cab5cb4]:
Loading required package: numDeriv
Loading 'lava' version 1.2.0
   Y          Z  eventtime  censtime      Noise       time event
1  0  2.0723063   2.832637  47.83940  0.2502561   2.832637     1
2  0  0.8351385  15.383153  74.49739  0.5577022  15.383153     1
3  1  0.8910657   6.788667  10.93360  1.1562815   6.788667     1
4  0 -0.4068996 778.850754 204.38587 -1.6876823 204.385869     0
5  1  0.6911767  15.419218 122.09289  1.1865709  15.419218     1
6  0 -1.1865021 161.020181  45.61059 -0.9373326  45.610587     0
7  1  0.2531404  40.717927  38.66130 -0.6073782  38.661303     0
8  0 -0.5618489  93.305275 188.84189  1.7607295  93.305275     1
9  0  0.9867950  47.883428  54.34103  0.0642331  47.883428     1
10 1 -0.3463124  24.506567  84.93882  1.6929759  24.506567     1

ud <- sapply(1:1000,function(s){
  d <- sim(m,50)
  f <- glm(Y~Z+Noise,data=d,family="binomial")
  coef(summary(f))[2,1]
})
mean(ud)

ud <- sapply(1:1000,function(s){
  d <- sim(m,50)
  library(survival)
  f <- coxph(Surv(time,event=="1")~Z+Noise,data=d)
  coef(summary(f))[1]
})
mean(exp(ud))






#+BEGIN_SRC R :exports code :results silent  :session *R* 
  library(lava)
  m <- lvm()
  eventtime <- "eventtime"
  censtime <- "censtime"
  latent(m) <- c(eventtime,censtime)
  distribution(m,"Z") <- normal.lvm()
  distribution(m,"X") <- binomial.lvm()
  distribution(m,"eventtime") <- coxWeibull.lvm(scale=1/100)
  distribution(m,"censtime") <- coxWeibull.lvm(scale=1/100)
  beta <- c(0.7,-0.1)
  gamma <- c(-0.1,-0.3)
  regression(m,to="eventtime",from=c("X","Z")) <- beta
  regression(m,to="censtime",from=c("X","Z")) <- gamma
  m <- eventTime(m,time~min(eventtime=1,censtime=0),"event")
  sim(m,10)
#+END_SRC

** Own distribution with manual parameter settings at sim call
#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
m <- lvm() 
distribution(m,~x,parname="p",init=0.1) <- function(n,p,...) rbinom(n,1,p) 
mean(sim(m,1000)$x)
mean(sim(m,1000,p=c(p=0.2))$x)
#+END_SRC

** Competing risks
#+BEGIN_SRC R  :results output raw  :exports results  :session *R* 
library(lava)
library(prodlim)
basicModel <- function(beta1,beta2,gamma){
  require(lava)
  m <- lvm()
  X <- paste("X",seq(12),sep="")
  timecause1 <- "timecause1"
  timecause2 <- "timecause2"
  censtime <- "censtime"
  latent(m) <- c(timecause1,timecause2,censtime)
  addvar(m) <- c(timecause1,timecause2,censtime,X)
  exogenous(m) <- X
  distribution(m,X[1:6]) <- normal.lvm()
  distribution(m,X[7:12]) <- binomial.lvm()
  distribution(m,timecause1) <- coxWeibull.lvm(scale=1/100)
  distribution(m,timecause2) <- coxWeibull.lvm(scale=1/100)
  distribution(m,censtime) <- coxWeibull.lvm(scale=1/100)
  regression(m,to=timecause1,from=X) <- beta1
  regression(m,to=timecause2,from=X) <- beta2
  regression(m,to=censtime,from=X) <- gamma
  m <- eventTime(m,time~min(timecause1=1,timecause2=2,censtime=0),"event")
  return(m)
}
beta1 <- rep(c(1,-1,0,0,1,-1),2) * log(2)
beta2 <- rep(c(0,0,1,-1,1,-1),2) * log(2)
gamma <- rep(0,12)
bmodel <- basicModel(beta1=beta1,beta2=beta2,gamma=gamma)
#+END_SRC

** Noise
#+BEGIN_SRC R  :results output raw  :exports results  :session *R* :cache yes 
addvar(bmodel) <- paste("Noise",1:12,sep="")
#+END_SRC

** Interaction

#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
beta <- c(1,1,-1,1)
m <- lvm()
distribution(m,"A") <- binomial.lvm()
distribution(m,"M") <- normal.lvm()
transform(m,M0~A+M) <- function(x){m <- x[["M"]]; m[x[["A"]]!=0] <- 0;m}
transform(m,M1~A+M) <- function(x){m <- x[["M"]]; m[x[["A"]]!=1] <- 0;m}
distribution(m,"Y") <- binomial.lvm() 
intercept(m,"Y") <- beta[1] 
regression(m,Y~A+M0+M1) <- beta[2:4]
d <- sim(m,1000)
summary(glm(Y~A*M,data=d,family="binomial"))
#+END_SRC
Yes, interactions have to be defined via 'transform', e.g., 

#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
m <- lvm()
regression(m,y~a+m+am) <- c(1,1,-1)
transform(m,am~a+m) <- prod
lm(y~a*m,sim(m,1e3))
#+END_SRC
In the newest version on github the following nested formula syntax is
also possible (parantheses are mandatory): 
#+BEGIN_SRC R  :results output raw drawer  :exports results  :session *R* :cache yes 
m <- lvm()
regression(m,Y~A+M+(AM~A*(M>0))) <- c(1,1,-1)
lm(y~a+m + I(m>0):a, sim(m,1e3))
#+END_SRC
   
** Transform


#+BEGIN_SRC R :exports both :results output raw  :session *R* 
  m <- lvm(y~x+z + xz)
  distribution(m,~z) <- binomial.lvm()
  transform(m,xz~x+z) <- prod
  cut0 <- function(x){if (x<0) 0 else 1}
  transform(m,cut.x~x) <- cut0
  d <- sim(m,100)
#+END_SRC


competing risks
#+BEGIN_SRC R :exports code :results silent  :session *R* 
m <- lvm()
Z <- paste("Z",seq(5),sep="")
X <- paste("X",seq(5),sep="")
timecause1 <- "timecause1"
timecause2 <- "timecause2"
censtime <- "censtime"
latent(m) <- c(timecause1,timecause2,censtime)
addvar(m) <- c(timecause1,timecause2,censtime,Z,X)
distribution(m,Z) <- normal.lvm()
distribution(m,X) <- binomial.lvm()
distribution(m,timecause1) <- coxWeibull.lvm(scale=1/100)
distribution(m,timecause2) <- coxWeibull.lvm(scale=1/100)
distribution(m,censtime) <- coxWeibull.lvm(scale=1/100)
m <- eventTime(m,time~min(timecause1=1,timecause2=2,censtime=0),"event")
sim(m,10)
#+END_SRC

#+BEGIN_SRC R :exports code :results silent  :session *R* 
regression(m,from="X1",to="timecause1") <- 4
d <- sim(m,1000)
d$status <- 1*(d$event==1)
coxph(Surv(time,status)~X1+Z1,data=d)
coxph(Surv(time,status==0)~X1+Z1,data=d)
#+END_SRC

** simulate data alike pbc data

*** pbc 

#+BEGIN_SRC R :exports both :results output raw drawer  :session *R* :cache yes 
u <- lvm(~age+x+v)
distribution(u,~x) <- binomial.lvm(p=0.4)
transform(u,b~age) <- function(x){x^2}
regression(u,b~v) <- 3.2
regression(u,x~age) <- log(3.2)
u <- categorical(u,~stage,labels=c("1/2","3","4"),K=3,p=c(.33,.25))
transform(u,stage3~stage) <- function(x){1*(x==1)}
regression(u,stage~age) <- list(log(3.2))
## publish(glm(stage3~age,data=sim(u,1000),family="binomial"))
publish(glm(x~age,data=sim(u,1000),family="binomial"))
publish(glm(b~v,data=sim(u,1000)))
#+END_SRC

#+BEGIN_SRC R  :results output   :exports results  :session *R* :cache yes 
library(survival)
library(prodlim)
library(lava)
library(rms)
data(pbc)
pbc$stage <- factor(pbc$stage)
levels(pbc$stage) <- list("1/2"=c(1,2),"3"=3,"4"=4)
pbc <- na.omit(pbc[,c("time","status","age","sex","stage","bili","protime","trt")])
pbc$logbili <- log(pbc$bili)
pbc$logprotime <- log(pbc$protime)
pbc$stage3 <- 1*(pbc$stage=="3")
pbc$stage4 <- 1*(pbc$stage=="4")
pbc$protimegrp <- cut(pbc$protime,c(-Inf,10,11,Inf),labels=c("<=10","10-11",">11"))
pbc$protimegrp1 <- pbc$protimegrp=="10-11"
pbc$protimegrp2 <- pbc$protimegrp==">11"
## f1 <- coxph(Surv(time,status==1)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4  ,data=pbc)
## f2 <- coxph(Surv(time,status==2)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4  ,data=pbc)
## g <- coxph(Surv(time,status==0)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4   ,data=pbc)
## parametric survival
F1 <- survreg(Surv(time,status==1)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=pbc)
F1a <- survreg(Surv(time,status==1)~sex+age+logbili+protimegrp+stage,data=pbc)
F2 <- survreg(Surv(time,status==2)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=pbc)
## G <- survreg(Surv(time,status==0)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=pbc)
G <- survreg(Surv(time,status==0)~1,data=pbc)
## logistic regression
M <- lvm()
distribution(M,~sex) <- binomial.lvm(p=mean(pbc$sex=="f"))
distribution(M,~age) <- normal.lvm(mean=50,sd=10)
distribution(M,~trt) <- binomial.lvm(p=.5)
distribution(M,~logbili) <- normal.lvm(mean=mean(pbc$logbili),sd=sd(pbc$logbili))
distribution(M,~logprotime) <- normal.lvm(mean=mean(pbc$logprotime),sd=sd(pbc$logprotime))
distribution(M,~time.transplant) <- coxWeibull.lvm(scale=exp(-F1$coef["(Intercept)"]/F1$scale),shape=1/F1$scale)
distribution(M,~time.death) <- coxWeibull.lvm(scale=exp(-F2$coef["(Intercept)"]/F2$scale),shape=1/F2$scale)
distribution(M,~time.cens) <- coxWeibull.lvm(scale=exp(-G$coef["(Intercept)"]/G$scale),shape=1/G$scale)
## bili coefficients
regression(M,logbili~age+sex) <- coef(lm(logbili~age+sex,data=pbc))[-1]
## protime coefficients
regression(M,protimegrp1~age+sex+logbili) <- coef(glm(protimegrp1~age+sex+logbili,data=pbc,family="binomial"))[-1]
regression(M,protimegrp2~age+sex+logbili) <- coef(glm(protimegrp2~age+sex+logbili,data=pbc,family="binomial"))[-1]
## stage coefficients
regression(M,stage3~age+sex+protimegrp1+protimegrp2+logbili) <- coef(glm(stage3~age+sex+protimegrp1+protimegrp2+logbili,data=pbc,family="binomial"))[-1]
regression(M,stage4~age+sex+protimegrp1+protimegrp2+logbili) <- coef(glm(stage4~age+sex+protimegrp1+protimegrp2+logbili,data=pbc,family="binomial"))[-1]
## survival coefficients
regression(M,time.transplant~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4) <- -coef(F1)[-1]/F1$scale
regression(M,time.death~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4) <- -coef(F2)[-1]/F2$scale
## regression(M,time.cens~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4) <- -coef(G)[-1]/G$scale
M <- categorical(M,~stage,labels=c("1/2","3","4"),K=3,p=c(mean(pbc$stage=="3"),mean(pbc$stage=="4")))
transform(M,stage3~stage) <- function(x){1*(x$stage==1)}
transform(M,stage4~stage) <- function(x){1*(x$stage==2)}
transform(M,protime~logprotime) <- function(x){exp(x)}
transform(M,protimegrp~protime) <- function(x){cut(x$protime, c(-Inf,10,11,Inf), labels=c("<=10","10-11",">11"))}
transform(M,protimegrp1~protimegrp) <- function(x){1*(x=="10-11")}
transform(M,protimegrp2~protimegrp) <- function(x){1*(x==">11")}
M <- eventTime(M,time~min(time.cens=0,time.transplant=1,time.death=2),"status")
d <- sim(M,10000)
d$sex <- factor(d$sex,levels=0:1,labels=c("m","f"))
sF1 <- survreg(Surv(time,status==1)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=d)
coxF1 <- coxph(Surv(time,status==1)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=d)
cbind(coef(F1),coef(sF1))
sF2 <- survreg(Surv(time,status==2)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=d)
cbind(coef(F2),coef(sF2))
## sG <- survreg(Surv(time,status==0)~sex+age+logbili+protimegrp1+protimegrp2+stage3+stage4,data=d)
## cbind(coef(G),coef(sG))
cbind(coef(glm(protimegrp1~age+sex+logbili,data=pbc,family="binomial")),coef(glm(protimegrp1~age+sex+logbili,data=d,family="binomial")))
cbind(coef(lm(logbili~age+sex,data=pbc)),coef(lm(logbili~age+sex,data=d)))
#+END_SRC



* install lava

cd /lava/pkg
make roxygen install pkg="lava"



